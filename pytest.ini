[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
asyncio_mode = auto
markers =
    unit: marca os testes unitários
    integration: marca os testes de integração
    slow: marca os testes que demoram para executar
